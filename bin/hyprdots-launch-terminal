#!/bin/bash

# hyprdots:summary=Launch a terminal in the active terminal's current directory
# hyprdots:args=[command...]

exec setsid uwsm-app -- xdg-terminal-exec --dir="$(hyprdots-cmd-terminal-cwd)" "$@"
