#!/bin/bash

# hyprdots:summary=Refresh the current theme from its templates.

THEME_NAME_PATH="$HOME/.config/hyprdots/current/theme.name"

if [[ -f $THEME_NAME_PATH ]]; then
  HYPRDOTS_THEME_SKIP_BACKGROUND=1 hyprdots-theme-set "$(cat $THEME_NAME_PATH)"
fi
