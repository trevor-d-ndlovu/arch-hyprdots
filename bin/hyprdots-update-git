#!/bin/bash

# hyprdots:summary=Pull the latest Hyprdots git changes

set -e

echo -e "\e[32mUpdate Hyprdots\e[0m"

hyprdots-update-time

# Suppress Hyprland config errors while git updates default config files mid-pull
hyprctl keyword debug:suppress_errors true &>/dev/null || true
hyprctl eval 'hl.config({ debug = { suppress_errors = true } })' &>/dev/null || true

git -C $HYPRDOTS_PATH pull --autostash
git -C $HYPRDOTS_PATH --no-pager diff --check || git -C $HYPRDOTS_PATH reset --merge

hyprctl reload &>/dev/null || true
