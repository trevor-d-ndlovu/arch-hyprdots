#!/bin/bash

# hyprdots:summary=Launch a TUI command in the default terminal with Hyprdots styling
# hyprdots:args=<command> [args...]

exec setsid uwsm-app -- xdg-terminal-exec --app-id=org.hyprdots.$(basename $1) -e "$1" "${@:2}"
