#!/bin/bash

# hyprdots:summary=Clear the internal-monitor-disable toggle if no external display is connected.

TOGGLE="$HOME/.local/state/hyprdots/toggles/hypr/internal-monitor-disable.lua"

if [[ -f $TOGGLE ]] && ! hyprdots-hw-external-monitors; then
  rm -f "$TOGGLE"
fi
