#!/bin/bash

# hyprdots:summary=Apply the current theme keyboard color to ASUS ROG keyboards
# hyprdots:hidden=true

ASUSCTL_THEME=~/.config/hyprdots/current/theme/keyboard.rgb

if hyprdots-cmd-present asusctl; then
  asusctl aura effect static -c $(sed 's/^#//' $ASUSCTL_THEME)
fi
