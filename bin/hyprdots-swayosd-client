#!/bin/bash

# hyprdots:summary=Wrapper for swayosd-client that targets the currently focused monitor.
# hyprdots:args=<swayosd-client-args...>

exec swayosd-client --monitor "$(hyprdots-hyprland-monitor-focused)" "$@"
